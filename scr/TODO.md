🗺️ ROADMAP: Пошаговый план реализации
✅ Этап 1: Базовая структура данных -- DONE

Storage: метод get_next_animal_id() — выдает следующий свободный ID
Animal: классы Animal, Cow, Bull, Calf с атрибутами (ID, имя, перки, возраст)
Animal: метод _generate_name() — генерация имени
Config: все константы (имена, перки, цены)

✅ Этап 2: Фабрика животных -- DONE

Animal: метод create_new_animal() — создает объект Cow/Bull/Calf с ID и именем

✅ Этап 3: Магазин (генерация товаров) -- IN PROGRESS

Market: метод generate_random_perks(quality) — генерирует перки (1-3 шт в зависимости от редкости)
Market: метод calculate_milk_production(perks) — рассчитывает молочность коровы
Market: метод create_animal_for_sale(type, quality) — создает животное через Animal.create_new_animal() + генерирует перки + сохраняет в БД
Market: метод calculate_price(animal, quality) — рассчитывает цену
Market: метод generate_starter_shop() — создает 5-7 животных для стартового магазина

✅ Этап 4: Storage (сохранение/загрузка)

Storage: метод save_animal(animal) — сохраняет животное в БД
Storage: метод add_market_offer(animal_id, price) — добавляет товар в магазин
Storage: метод get_market_offers() — получает список всех товаров магазина
Storage: метод load_animal(animal_id) — загружает животное из БД

✅ Этап 5: Игровой цикл

Game: метод start_new_game() — инициализация новой игры
Game: метод show_market() — отображает товары магазина
Game: метод buy_animal(animal_id) — покупка животного из магазина
Pen: метод add_animal(animal, x, y) — размещение животного в загоне
Game: метод save_game() — сохранение игры
Game: метод load_game() — загрузка игры

✅ Этап 6: Базовый функционал животных

Cow: метод produce_milk(hours) — производство молока
Game: метод collect_milk() — сбор молока со всех коров
Game: метод feed_animals() — кормление всех животных
Animal: метод age_up(hours) — увеличение возраста

✅ Этап 7: Размножение (потом)

GeneticSystem: метод inherit_perks() — наследование перков
GeneticSystem: метод determine_gender() — определение пола теленка
Game: метод breed_animals(cow_id, bull_id) — скрещивание